# 冒险岛 079 复刻项目 - 版本一致性检查清单

> **目标**: 确保项目内容与 MapleStory 079 版本（国际服 v0.79，2008 年前后）保持一致。
> **检查时间**: 周期 #20 审计（2026-06-14）
> **更新周期**: 每小时自动化检查

---

## 📋 核心数据清单

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 6大职业初始属性 | ✅ | 新手/战士(100)/法师(200)/弓箭手(300)/飞侠(400)/海盗(500) — `pkg/utils/constants.go` |
| 主要地图数据 | ✅ | 40+ 张核心地图 — `scripts/init_data.go seedMaps()` |
| 常见怪物数据 | ✅ | 1级蜗牛 → 80级 BOSS — `scripts/init_data.go seedMobs()` |
| 升级经验公式 | ✅ | `10 + Level² × 8` — `pkg/utils/constants.go` |
| 升级HP/MP奖励 | ✅ | 职业差异化 `getLevelUpHPMP()` 已实现 |
| 技能/能力点分配 | ✅ | 10级起每级3 SP，每级5 AP |
| BGM资源清单 | ✅ | 清单完整 — `client/assets/audio/README.md` |
| UI贴图资源 | ⚠️ | 清单完整，**实际文件缺失**（需从 WZ 提取） |
| 精灵动画资源 | ⚠️ | 清单完整，**实际文件缺失**（需从 WZ 提取） |
| API 战斗/物品/NPC | ✅ | 周期 #20 已补全 routes（见下表） |
| REST 响应格式 | ⚠️ | handler 与 client 字段包装不一致，待任务 5 |
| WebSocket 实时通信 | ✅ | 已支持嵌套 payload + room/channel 参数 |
| 编译测试验证 | ✅ | `go vet` / `go build` / `go test` 全部通过 |

---

## 🔌 API 联调状态（周期 #20 更新）

| 接口路径 | 方法 | 状态 | 说明 |
|---------|------|------|------|
| `/api/v1/combat/calculate-damage` | POST | ✅ | 新增 |
| `/api/v1/combat/player-attack-mob` | POST | ✅ | 新增 |
| `/api/v1/combat/mob-attack-player` | POST | ✅ | 新增 |
| `/api/v1/combat/stats` | GET | ✅ | 新增 |
| `/api/v1/combat/revive` | POST | ✅ | 新增 |
| `/api/v1/items` | GET | ✅ | 新增 |
| `/api/v1/items/:id` | GET | ✅ | 新增 |
| `/api/v1/npcs/map/:mapId` | GET | ✅ | 新增 |
| `/api/v1/npcs/interact/:id` | POST | ✅ | 新增 |
| `/api/v1/game/move` | POST | ❌ | 客户端调用，路由未注册 |
| `/api/v1/game/levelup/:id` | POST | ❌ | 客户端调用，路由未注册 |
| `/api/v1/game/add-ap` | POST | ❌ | 客户端调用，路由未注册 |
| `/api/v1/game/restore` | POST | ❌ | 客户端调用，路由未注册 |

---

## 🎨 客户端资源（079 对齐）

### 资源来源
079 客户端资源存储于 **WZ(PKG1)** 二进制格式：
- **Sound.wz** — BGM/SFX（登录、城镇、野外、BOSS）
- **Map.wz** — 地图瓦片与背景
- **Character.wz / Mob.wz / Npc.wz** — 精灵动画
- 推荐工具：[HaRepacker-resurrected](https://github.com/lastbattle/Harepacker-resurrected)、[MapleLib](https://github.com/lastbattle/MapleLib)

### 当前状态
| 目录 | 清单 | 实际文件 | 079 对齐 |
|------|------|---------|---------|
| `client/assets/audio/` | ✅ 36项 | ❌ 0 | 待提取 |
| `client/assets/images/` | ✅ 完整 | ❌ 0 | 待提取 |
| `client/assets/sprites/` | ✅ 完整 | ❌ 0 | 待提取 |

### 参考源码
- `../mapleStory079-external/10-cc-079-ms-Java079参考/` — Java 079 私服参考（地图/NPC/怪物 ID）
- `../mapleStory079-external/06-ZLHSS2-079参考-中文全栈私服/` — 备选参考

---

## 📝 后续任务（写入 .next_tasks.md）

1. **任务 4** — 从 WZ 提取资源到 client/assets/
2. **任务 5** — REST 响应格式统一
3. **任务 6** — GameProvider 双向绑定
4. **任务 8** — game/move 等 4 个缺失路由

---

_每小时审计自动化维护此文件。_
